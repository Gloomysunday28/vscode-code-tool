# martin-code-tool
```javascript
clg => console.log('martin', 'martin') /* 2019年10月15日15:17:37 */
fb => for (let key in object) {
  console.log('key', object[key])
}
each => Array.forEach((value, index) => {
  console.log('key', object[key])
}
map => Array.map((value, index) => {
  console.log('key', object[key])
}
```